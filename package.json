{
  "name": "vettura-app",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "heroku-prebuild": "yarn build:client && yarn build:server",
    "build:client": "cd client && yarn && yarn build",
    "build:server": "cd server && yarn",
    "start:client": "cd client && yarn start",
    "start:server": "cd server && yarn start"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/woophi/drivedrivedrive"
  },
  "cacheDirectories": ["client/node_modules", "server/node_modules"],
  "engines": {
    "node": "8.11.1",
    "yarn": "1.6.0"
  }
}
