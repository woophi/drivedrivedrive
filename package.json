{
  "name": "vettura-app",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "heroku-prebuild": "npm run-script build:client && npm run-script build:server",
    "build:client": "cd client && npm -i && rimraf yarn.lock && npm run-script build",
    "build:server": "cd server && npm -i"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/woophi/drivedrivedrive"
  },
  "cacheDirectories": ["client/node_modules", "server/node_modules"],
  "engines": {
    "node": "8.11.1",
    "yarn": "1.9.4"
  }
}
